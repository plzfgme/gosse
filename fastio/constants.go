package fastio

const (
	// KeySize is the key size of the client.
	KeySize = 32
	// MaxIDSize is the max id size used in the scheme.
	MaxIDSize = 30
)

const eSize = 32

const (
	flagAdd    byte = 0
	flagDelete byte = 1
)
